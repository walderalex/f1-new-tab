{
  "name": "f1-new-tab-extension",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix newtab",
    "dev": "npm run dev --prefix newtab",
    "package": "npm run build && zip -FSr extension.zip manifest.json Icon.png newtab/dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.191"
  }
}
